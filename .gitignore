# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/

# env files and secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*.pfx
secrets/
*.secret

# prisma
# NOTE: prisma/migrations/ is intentionally NOT ignored — `prisma migrate deploy`
# replays committed migration files in production.

# misc
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# verification builds (npm run build:check)
.next-check

# Playwright MCP session artifacts (screenshots, traces)
.playwright-mcp/

# Claude Code notes and local settings
CLAUDE.md
**/CLAUDE.md
CLAUDE.local.md
**/CLAUDE.local.md
.claude/
